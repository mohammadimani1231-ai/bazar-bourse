# پروژه: داشبورد تحلیل بازار بورس ایران + جهانی (تک‌کاربره، شخصی)

## استک
Next.js 14+ App Router + TypeScript + Tailwind روی Vercel (فقط UI).
Supabase = قلب سیستم: Postgres، Edge Functions (Deno)، pg_cron + pg_net زمان‌بند، Realtime.

## قیدهای سخت — هرگز نقض نشود
1. Vercel Cron ممنوع (پلن Hobby فقط کران روزانه می‌دهد). همه زمان‌بندی‌ها pg_cron
   داخل Supabase که با pg_net به Edge Function ها HTTP می‌زند. کران‌ها UTC هستند؛
   بازار تهران شنبه‌تاچهارشنبه ۹:۰۰-۱۳:۰۰ تهران = ۵:۳۰-۹:۳۰ UTC.
2. RLS روی همه جدول‌ها فعال. نوشتن فقط با service_role (فقط در Edge Function / env).
   کلید anon فقط SELECT. service_role هرگز در کد فرانت یا git.
3. محاسبات اندیکاتور/متریک فقط روی کندل بسته‌شده (ضد repainting). منطق بک‌تست
   باید عیناً همان توابع lib/ منطق زنده را import کند — پیاده‌سازی موازی ممنوع.
4. آستانه‌های مبلغی (مثل پول درشت) پرسنتایلی، نه عدد ثابت تومانی (تورم).
5. هر collector: timeout + retry با backoff + ثبت وضعیت در pipeline_health.
   خطای یک منبع نباید بقیه را متوقف کند.
6. هر امتیاز/سیگنال باید تفکیک فاکتورهایش را ذخیره کند (jsonb reasons) — جعبه‌سیاه ممنوع.
7. LLM هرگز در مسیر صدور سیگنال نیست؛ فقط تفسیر زمینه (فاز ۶).
8. تاریخ در DB همیشه UTC/گرگوریان؛ در UI شمسی (date-fns-jalali).
9. در نمایش قیمت TSE همیشه هر دو: آخرین قیمت (pl) و قیمت پایانی (pc) — حجم مبنا
   این دو را جدا می‌کند.
10. commit های کوچک روی برنچ dev؛ merge به main فقط با تأیید کاربر. **ایمیل گیت روی این ریپو**
    (`git config user.email` محلی، نه global) عمداً `mohammadi.mani1231@gmail.com` است — همان
    ایمیل اکانت Vercel `mohammadimani1231-ai` که پروژهٔ `bazar-bourse` رویش است. پلن Hobby
    اجازهٔ عضو تیم نمی‌دهد، پس اگر author کامیت این ایمیل نباشد Vercel deploy را روی push خودکار
    block می‌کند («Git author ... must have access to the team»). این عمداً جدا از ایمیل global
    (`mohammadi.davoud1351@gmail.com`، مال ریپوهای دیگر مثل piro-admin) نگه داشته شده — دستکاریش نکن.
11. وضعیت بازار هرگز از حافظه یا فرمول «شنبه تا چهارشنبه» استنتاج نشود. تعطیلات بورس ایران
    شامل تعطیلات قمری متغیر و تعطیلی‌های اعلامی بی‌قاعده است. `lib/market-status.ts` منبع
    حقیقت است: اول جدول `market_holidays` (کمکی، کاربر پر می‌کند)، بعد اگر حجم نماد پرمعامله
    در ۱۵ دقیقهٔ اخیرِ بازهٔ نظری بازار تغییر نکرده باشد → بازار بسته فرض شود. هر Edge Function
    مربوط به بازار (collect-tse، health-check، compute-tabloo، compute-signals، build-candles،
    compute-rank) باید ابتدا این را چک کند؛ اگر بسته بود، ساکت با status='market_closed' در
    pipeline_health خارج شود، نه error. health-check هشدار «مرگ پایپ‌لاین» را فقط وقتی بازار
    باز است بدهد. برای Claude Code: هر جا به «باز بودن بازار» نیاز بود همین تابع/جدول را چک کن،
    هرگز از روز هفته یا دانش عمومی دربارهٔ تقویم ایران نتیجه‌گیری نکن.

## منابع دیتا
- بورس ایران: BrsApi.ir (env: BRSAPI_KEY) — پایه `https://Api.BrsApi.ir` (از ۱۴۰۵/۰۲/۰۵ عوض شده، لینک قدیمی `BrsApi.ir/Api` از ۱۴۰۵/۰۵/۰۵ حذف می‌شود)
  - تک‌نماد: `/Tsetmc/Symbol.php?key=...&l18=<نماد فارسی>` — سهمیهٔ روزانه‌اش بسیار کم است، برای پایپ‌لاین استفاده نشود
  - همهٔ نمادها یک‌جا (برای collect-tse): `/Tsetmc/AllSymbols.php?key=...`
  - طلا/سکه/ارز (نرخ بازار آزاد، نه دولتی/نیمایی): `/Market/Gold_Currency.php?key=...`
- طلا/ارز ریالی: BrsApi (fallback: TGJU) — فقط نرخ بازار آزاد؛ نرخ دولتی/رسمی فعلاً خارج از scope است
- جهانی: Yahoo Finance غیررسمی https://query1.finance.yahoo.com/v8/finance/chart/{symbol}
  (BZ=F برنت، GC=F انس، HG=F مس، DX-Y.NYB، ^GSPC) — پشت adapter تا تعویض آسان باشد
- تاریخی TSE: اسکریپت seed یک‌بارمصرف Python با pytse-client (نیازمند Python ≤3.12 — lxml هنوز روی 3.14 wheel ندارد)
- تاریخی حقیقی/حقوقی (پیش‌نیاز متریک پول درشت): `/Tsetmc/History.php?key=...&type=1&l18=...` (تاریخ Jalali برمی‌گرداند).
- تاریخی دلار آزاد/طلای ۱۸ عیار (پیش‌نیاز بنچمارک بک‌تست): `/Market/Gold_Currency_Pro.php?key=...&section=currency|gold&history=2&symbol=USD|IR_GOLD_18K&date_start=...&date_end=...` (تاریخ Jalali، فرمت `date_end` باید `YYYY-MM-DD` امروزِ واقعی باشد نه تاریخ آینده وگرنه 400 می‌دهد).
- تاریخی شاخص کل (tedpix): `pytse_client.download_financial_indexes(symbols="شاخص كل")` (کاف/یای عربی، نه فارسی).
  ⚠️ هر دو endpoint بالای BrsApi (History.php و Gold_Currency_Pro.php) از IP بعضی محیط‌های ابری
  (مثل sandbox اجرای Claude Code) توسط BrsApi ریست می‌شوند — اگر backfill محلی fail شد، از
  Edge Function معادل (`backfill-buyer-breakdown`, `backfill-benchmarks` — هر دو از IP خود Supabase) استفاده کن.

## وضعیت فازها
- [x] فاز ۰: زیرساخت + تست روز صفر
- [x] فاز ۱: پایپ‌لاین دیتا
- [x] فاز ۲: موتور تابلوخوانی — اعتبارسنجی زندهٔ فولاد/فملی/وبملت هنوز مونده، منتظر یک روز معاملاتی واقعی (نه تعطیل)
- [x] فاز ۳: موتور سیگنال + رتبه مرکب + بک‌تست — نتیجهٔ صادقانهٔ بک‌تست ۵ ساله (2021-01-01..2026-08-03):
      با قوانین اولیه ۶۵ معامله/بازده -۲۶٪؛ بعد از تنظیم وزن بر اساس همین بک‌تست، ۱۲ معامله/بازده +۰.۴۶٪.
      در هر دو حالت به‌شدت از buy-and-hold شاخص/دلار/طلا عقب‌تر است و نمونهٔ معاملات (۱۲-۶۵) برای
      نتیجه‌گیری آماری معتبر خیلی کوچک است — این سیستم فعلاً مدرکی برای alpha واقعی روی سهام ندارد.
      قبل از استفادهٔ واقعی، فاز‌های بعد باید این را جدی بگیرند، نه صرفاً چک‌لیست را تیک بزنند.
- [x] فاز ۴: داشبورد UI — هر ۶ صفحه (`/`, `/symbol/[symbol]`, `/signals`, `/screener`, `/global`, `/health`) ساخته
      و روی build/tsc/eslint/vitest (140 تست) تمیز رد شد. نکات مهم:
      - **دو باگ واقعی کشف و رفع شد حین ساخت:** (۱) همهٔ صفحات دیتای زنده بدون `export const dynamic =
        "force-dynamic"` به‌صورت static در build-time prerender و freeze می‌شدند — یعنی داشبورد deploy‌شده
        دیتای منجمد زمان build را تا deploy بعدی نشان می‌داد؛ اضافه شد به هر ۶ صفحه. (۲) PostgREST سقف
        پیش‌فرض ۱۰۰۰ ردیف دارد و `.limit()` بزرگ‌تر را ساکت نادیده می‌گیرد — کوئری چندنمادی/چنددارایی
        `/global` (order by date/captured_at بدون تفکیک asset) داشت فقط ۱۰۰۰ ردیف اول را برمی‌گرداند و
        نمودار rebase را خالی نشان می‌داد؛ با `fetchAllPages` (صفحه‌بندی `.range()`) در `app/global/page.tsx`
        رفع شد. این کلاس باگ (سقف ۱۰۰۰ ردیف Supabase) قبلاً هم در فاز ۳ کشف شده بود — یادآوری برای فازهای بعد.
      - **شکاف‌های اسکیما نسبت به پرامپت (گزارش‌شده و مستند، نه حدس زده):** نه «ارزش بازار» (مارکت‌کپ) و نه
        «شاخص هم‌وزن» در هیچ جدولی وجود ندارد — treemap/اسکرینر به‌جای مارکت‌کپ از ارزش معاملات لحظه‌ای
        استفاده می‌کنند (برچسب دقیق زده شده، تصمیم با کاربر گرفته شد)؛ شاخص هم‌وزن در UI با placeholder
        «دادهٔ موجود نیست» نشان داده می‌شود. «شاخص پالایشی» و «فلزات اساسی» برای پنل lead-lag داده نداشتند —
        با `lib/syntheticIndex.ts` (ترکیب هم‌وزن rebase-شدهٔ نمادهای همان صنعت در واچ‌لیست) جایگزین و برچسب‌گذاری
        شفاف شدند. برنت/انس‌طلا/DXY فقط از `global_quotes` (تازه فعال‌شده، همین چند روزه) می‌آیند نه یک جدول
        تاریخی — نمودار rebase/heatmap برایشان فعلاً کم‌داده است، ظرف چند هفته با جمع‌شدن دیتا کامل می‌شود.
      - `composite_rank` و `signals` هنوز خالی‌اند (اولین اجرای cron شبانه هنوز نیفتاده بود) — صفحات مربوطه
        empty-state درست نشان می‌دهند، رفتار مورد انتظار است نه باگ.
      - RSI۱۴/رتبهٔ مرکب/فاصله از MA۵۰ در اسکرینر به‌صورت زنده از `lib/composite-rank.ts` (همان تابع پایپ‌لاین
        زنده) روی `daily_candles` هر نماد محاسبه می‌شود، نه از جدول (که خالی بود) — طبق قاعدهٔ «بدون پیاده‌سازی موازی».
      - اعتبارسنجی زندهٔ فولاد/فملی/وبملت (فاز ۲) هنوز باز است — همچنان منتظر یک روز معاملاتی واقعی برای چک دستی.
- [x] فاز ۵: لایه ژئوپلیتیک + هشدار تلگرام — سه مکانیزم + هشدار ساخته و روی build/tsc/eslint/vitest
      (۱۷۹ تست) تمیز رد شد:
      - `compute-tension` (هر ۱۵ دقیقه، مستقل از ساعات بازار): z-score نوسان دلار + حباب سکه امامی +
        z-score تغییر برنت → gauge ۰-۱۰۰ در `global_quotes(asset='tension_index')`. sanity-check تاریخی
        واقعی نوشته شد (تست `lib/tension.test.ts`): بزرگ‌ترین جهش واقعی دلار در دادهٔ پروژه (۲۰۲۵-۰۶-۲۵،
        افت ۱۱.۳٪) z-score≈۵.۲ می‌دهد — رفتار درست و تأییدشده. **محدودیت شناخته‌شده:** مؤلفهٔ حباب سکه
        فعلاً عدد غیرمعقول بزرگ می‌دهد (gold_18k/coin_emami در برابر gold_ounce×usd_irr آربیتراژ نمی‌شوند)؛
        چون منشأش قطعی پیدا نشد (شاید عدم‌همزمانی snapshot تاریخی benchmark_candles با global_quotes زنده،
        نه لزوماً باگ)، gauge کلمپ‌شده امن است ولی مقدار مطلقش قابل‌اتکا نیست — قبل از استفادهٔ جدی از این
        مؤلفه باید دوباره بررسی شود.
      - **یک باگ واقعی کشف و رفع شد حین ساخت همین مؤلفه:** BrsApi `Gold_Currency.php` آرایهٔ `currency`
        (usd_irr) را به تومان می‌دهد ولی آرایهٔ `gold` (gold_18k/coin_emami) را به ریال — ناسازگاری واقعی
        در خود API، نه اشتباه ما. از فاز ۱ تا الان `global_quotes.usd_irr` ده برابر کوچک‌تر از
        `benchmark_candles.usd_irr` بود (کشف شد چون tension_index این دو را برای اولین بار مستقیم مقایسه
        کرد). رفع شد در `lib/transforms/globalQuote.ts` (×۱۰ فقط روی آرایهٔ currency) + مایگریشن
        `20260804160000` دادهٔ قبلی global_quotes را یک‌بار اصلاح کرد (idempotent با گارد schema_log).
        دیتای تیکر جهانی فاز ۴ («دلار آزاد») از همین تاریخ درست می‌شود — قبلش ۱۰ برابر کوچک‌تر نمایش داده می‌شد.
      - `collect-news` (ساعتی): فیدهای RSS واقعی تست‌شده زنده (دنیای اقتصاد، اقتصاد آنلاین، ایسنا، مهر،
        Tehran Times، Al Jazeera — همه در `settings.news_feeds`، configurable). پارسر RSS دست‌ساز
        (`lib/rss.ts`، بدون وابستگی خارجی) روی نمونهٔ واقعی تست شد. روی دادهٔ زندهٔ همین امروز ۳۷ خبر مرتبط
        (تنش هرمز، مذاکرات ایران-آمریکا، مصرف موشک...) گرفت و ذخیره کرد — کیفیت تطبیق کلیدواژه تأیید شد.
      - `settings(market_regime)` ساخته شد؛ سوییچ رژیم در نمای کلی + بنر در همهٔ صفحات (در root layout) کار
        می‌کند و زنده تست شد (روی مرورگر: سوییچ به تنش → بنر ظاهر شد → برگشت به عادی → بنر ناپدید شد).
        `compute-signals` دیگر رژیم را هاردکد `normal` نمی‌کند، از `settings` می‌خواند (آستانهٔ خرید ۶۰ در
        رژیم غیرعادی، طبق فاز ۳، از الان واقعاً وصل است).
      - `evaluate-alerts` (۱۰دقیقه‌ای، action: سیگنال جدید/جهش تنش/مرگ پایپ‌لاین) و `send-alert-digest`
        (ساعتی، info: حجم مشکوک/پول درشت/کد‌به‌کد — **همه در یک پیام**، طبق طراحی ضد نویز) دیپلوی و روی
        دادهٔ زنده تست شدند. secretهای تلگرام (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`/`SITE_URL`) با
        `supabase secrets set` ست شدند و **ارسال واقعی تأیید شد**: یک دایجست info از دادهٔ واقعی جلسهٔ
        معاملاتی ۱۴۰۵/۰۵/۱۳ (آریا/ارفع حجم مشکوک، آلومینا کد‌به‌کد) با `delivered=true` در `alert_log` رسید.
      - **باگ نویز کشف و رفع شد (مهم، چون کل ارزش ماژول هشدار به ضدنویز بودنش است):** سر هر ساعت که چند
        کرون هم‌زمان شلیک می‌کنند، `evaluate-alerts` گاهی با `PGRST303 JWT issued at future` (اسکیوی ساعت
        زیر بار، سمت زیرساخت Supabase) خطا می‌خورد؛ ۱۰ دقیقه بعد **خودش خطای خودش را می‌دید** و هشدار
        «مرگ پایپ‌لاین» می‌فرستاد — یک حلقهٔ خودارجاع که هر ساعت یک هشدار کاذب می‌ساخت. رفع: قانون
        `pipeline_health` حالا فقط سورس‌هایی را گزارش می‌کند که **آخرین وضعیتشان** خطاست (نه هر خطای گذرا
        در بازه)، و خود `evaluate-alerts` از این چک کنار گذاشته شد. بعد از رفع، اجرای دستی `fired: []` داد
        (درست — چون واقعاً هیچ سورسی خراب نیست).
      - `send-alert-digest` پارامتر اختیاری `{"sinceMinutes": N}` در بدنه می‌گیرد (فقط برای اجرای دستی،
        مثلاً دایجست کل یک جلسهٔ معاملاتی بعد از بسته‌شدن بازار)؛ کرون بدنه نمی‌فرستد و از watermark استفاده می‌کند.
      - **بخش تأییدنشدهٔ DoD:** هشدار **action** واقعی هنوز در تلگرام دریافت نشده — چون هیچ‌کدام از شرایطش
        الان صادق نیست (بازار بسته، سیگنال جدیدی نیست، تنش زیر آستانه، پایپ‌لاین سالم). مسیر ارسال دقیقاً
        همان `sendTelegramMessage` است که با دایجست تأیید شد، پس زیرساخت اثبات‌شده است؛ ولی اولین هشدار
        action واقعی باید در جلسهٔ معاملاتی بعدی رصد شود.
- [x] فاز ۶: لایه AI (بریف روزانه) — کد کامل، build/tsc/eslint/vitest (۱۹۷ تست) تمیز رد شد.
      **تنها بخش تأییدنشده: خود فراخوانی Claude API واقعی** — چون credit حساب Anthropic هنوز
      فعال نشده بود (خرید شد ولی activation یک روزه بود). کل پایپ‌لاین قبل از آن نقطه (ساخت
      ورودی ساخت‌یافته از ۷ بخش DB، فراخوانی، خطای واقعی ۴۰۰ credit، لاگ در pipeline_health)
      زنده تست و تأیید شد — یعنی تنها چیزی که آزمایش نشده خودِ پاسخ مدل و اعتبارسنجی zod روی
      یک پاسخ واقعی است (منطق پارس/zod با نمونهٔ دستی‌ساختهٔ دقیقاً هم‌فرمت prompt تست شده،
      نه واقعی از Claude). فردا با فعال‌شدن credit باید یک اجرای واقعی تأیید شود.
      - `daily-brief` (کرون `0 5 * * 6,0,1,2,3` = ۸:۳۰ تهران) ورودی را از ۷ بخش می‌سازد:
        global (برنت/انس/مس/DXY/S&P500 + تغییر ۲۴س/۷روزه از global_quotes)، domestic (دلار/طلای‌۱۸/حباب
        سکه از lib/tension.ts)، tension_index + market_regime، market (شاخص کل/هم‌وزن — حالا واقعی، طبق
        رفع‌شدن فاز ۴ — «ارزش معاملات خرد» چون تفکیک ندارد صریحاً به‌جایش «ارزش کل معاملات» فرستاده می‌شود)،
        signals (۲۴ساعت اخیر با reasons)، news (۲۴ساعت اخیر)، correlation_breaks (دلار×فلزات و برنت×پالایشی
        با `lib/correlationBreaks.ts` روی همان ترکیب هم‌وزن صنعتی فاز ۴).
      - اعتبارسنجی خروجی با zod (`lib/briefSchema.ts`) + یک retry خودکار + شکست نهایی در pipeline_health
        (نه insert جزئی/نامعتبر در ai_briefs).
      - System prompt عیناً طبق متن پرامپت فاز ۶، بدون تغییر.
      - **مشکل فنی کشف و رفع شد:** `lib/briefSchema.ts` (مشترک Next.js+Deno) با `import { z } from "zod"`
        در Deno با خطای «Relative import path not prefixed with /» شکست می‌خورد چون Deno بدون import map
        بارهٔ package name را نمی‌شناسد. رفع: `supabase/functions/deno.json` با نگاشت `"zod":"npm:zod@^4"` +
        `[functions.daily-brief] import_map=...` در `config.toml` (خودکار روی هر deploy بعدی اعمال می‌شود،
        نیازی به `--import-map` دستی نیست).
      - UI: کارت بریف در نمای کلی (mood + summary + ریسک اصلی، خلاصه) پر شد (placeholder فاز ۴ حذف شد)،
        صفحهٔ `/briefs` برای آرشیو کامل با برچسب اطمینان رنگی و tooltip قابل‌بازشدن روی هر `ref` (نمایش خام
        همان بخش input_snapshot که ادعا به آن ارجاع داده — بر اساس بخش سطح‌بالا، نه مسیر دقیق، چون فرمت آزاد
        `ref` را نمی‌شود کاملاً پیش‌بینی کرد). دکمهٔ «توضیح بده» در `/signals` بدون LLM کار می‌کند
        (`lib/signalExplain.ts`، template متنی از reasons).
- [x] فاز ۷: ماژول گزارش‌گیری تحلیلی — کد کامل، build/tsc/eslint/vitest (۲۲۱ تست) تمیز رد شد.
      جدول `reports (type, period, html, data_snapshot, created_at)` + RLS select-عمومی. گزارش‌ها
      HTML ثابتِ سمت‌سرور هستند (بدون وابستگی JS کلاینتی، طبق طراحی «قابل چاپ به PDF»):
      `lib/reportCharts.ts` (SVG bar/line دست‌ساز) + `lib/reportHtml.ts` (قالب جدول/بخش/شِل با
      `@media print` که فقط `.report-root` را چاپ می‌کند) + `lib/jalaliMonth.ts`/`lib/seasonality.ts`
      (فصلی‌نگری ماه شمسی) + `lib/weeklyBriefSchema.ts` (zod خلاصهٔ اجرایی، `ConfidenceLevel` مشترک
      با `lib/briefSchema.ts` فاز ۶).
      - **گزارش هفتگی** (`weekly-report`، کرون `30 6 * * 4` = ۱۰:۰۰ پنجشنبهٔ تهران): هر ۸ بخش
        (خلاصهٔ اجرایی LLM، بازار در یک نگاه، جریان پول، زمینهٔ جهانی، رژیم و تنش، کارنامهٔ موتور
        سیگنال، هفتهٔ پیش رو، رتبه‌بندی واچ‌لیست) با تجدید استفادهٔ کامل از توابع پایپ‌لاین زنده
        (`ruleStats`، `composite-rank`، `correlationBreaks`، `syntheticIndex`، `downsampleDaily`،
        `tension`) طبق قاعدهٔ «بدون پیاده‌سازی موازی». پیام تلگرام «گزارش هفتگی جدید» خارج از دایجست،
        طبق طراحی.
      - **مشکل فنی کشف و رفع شد:** `date-fns-jalali` (وارد شده از `lib/jalali.ts`) در Deno با همان
        خطای «package name نامعتبر» زود که `zod` در فاز ۶ داشت شکست می‌خورد؛ نسخهٔ نصب‌شده
        پیش‌انتشار است (`4.4.0-0`) و semver `^4` روی prerelease مچ نمی‌شود — رفع با پین دقیق
        `"date-fns-jalali": "npm:date-fns-jalali@4.4.0-0"` در `supabase/functions/deno.json`.
      - **باگ واقعی کشف و رفع شد:** وقتی `usd_irr`/`coin_emami` در طول هفته فقط یک رکورد `global_quotes`
        داشتند (وقفهٔ شناخته‌شدهٔ BrsApi از فاز ۵)، بخش «زمینهٔ جهانی» عدد گمراه‌کنندهٔ «۰٫۰٪» نشان
        می‌داد (چون اولین و آخرین رکورد هفته یکی بودند). رفع: گارد `weekRows.length < 2` که به‌جایش
        «—» نشان می‌دهد. با اجرای مجدد زندهٔ تابع تأیید شد (گزارش id=5، ستون دلار آزاد/سکه امامی
        درست «—» شد). **علت ریشه‌ای (خرابی متناوب endpoint طلا/ارز BrsApi در `collect-global`) هنوز
        رفع نشده** — این فقط لایهٔ نمایش را اصلاح کرد، نه پایپ‌لاین جمع‌آوری را.
      - یک گلیچ گذرای پلتفرم Supabase Edge (اولین اجرا، ستون شاخص کل خالی برگشت با اینکه کوئری و
        دیتا هر دو سالم بودند) در اجرای دوم رفع شد بدون تغییر کد — همان الگوی شناخته‌شدهٔ ناپایداری
        از فاز ۵ (`evaluate-alerts`/`JWT issued at future`)، تکرار نشد.
      - **گزارش عمیق تک‌نماد** (`generateSymbolReport` در `app/symbol/[symbol]/actions.ts`، Server
        Action): همان ۸ بخش پرامپت (پروفایل، عملکرد قیمت adjusted + نمای دلاری + مقایسه با شاخص
        صنعت/کل، تابلوخوانی ۳۰روزه، رفتار صف، تاریخچهٔ سیگنال، همبستگی با برنت/دلار/انس با CCF و لگ
        بهینه از `lib/stats.ts`، فصلی‌نگری، جمع‌بندی LLM با جملهٔ ثابت پایانی). دکمهٔ «تولید گزارش
        عمیق نماد» در صفحهٔ نماد. **تست زنده روی دو نماد واقعی:** خودرو (پرمعامله — همبستگی و
        فصلی‌نگری واقعی محاسبه شد) و آلومینا (کم‌داده، ۲۷۴ کندل — بدون کرش، همه‌جا «داده کافی نیست»/
        «داده‌ای برای این بخش نیست» درست نمایش داده شد).
      - صفحهٔ `/reports`: آرشیو با فیلتر نوع + جستجوی نماد (`ilike` روی `period`) در نمای فهرست،
        نمای تک‌گزارش با دکمهٔ چاپ (`window.print()`، همان print-stylesheet تعبیه‌شده در HTML ذخیره‌شده).
        لینک «گزارش‌ها» به `Nav.tsx` اضافه شد.
      - **سناریوی بدون LLM طبیعتاً روی هر دو نوع گزارش تست شد:** چون `ANTHROPIC_API_KEY` هنوز نه در
        Vercel env و نه فعال روی حساب Anthropic است، هم گزارش هفتگی و هم گزارش‌های تک‌نماد بدون خلاصهٔ
        هوش‌مصنوعی و با پاراگراف fallback مشخص تولید شدند — بقیهٔ گزارش کامل و سالم ماند.
      - **بخش تأییدنشدهٔ DoD:** تحویل واقعی پیام تلگرام «گزارش هفتگی جدید» در چت کاربر مستقیماً چک
        نشد (این مسیر خارج از `alert_log` است، پس رکورد دیتابیسی هم ندارد که بشود چکش کرد).
      - **کار باقی‌مانده برای بعد:** افزودن `ANTHROPIC_API_KEY` به Vercel env (فعلاً فقط در Supabase
        secrets است، که برای Edge Functionها کافی است ولی گزارش تک‌نماد چون از سمت Next.js/Vercel
        فراخوانی می‌شود کلید جدا لازم دارد).
(در پایان هر فاز این چک‌لیست را آپدیت کن.)
