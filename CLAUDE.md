# پروژه: داشبورد تحلیل بازار بورس ایران + جهانی (تک‌کاربره، شخصی)

## استک
Next.js 14+ App Router + TypeScript + Tailwind روی Vercel (فقط UI).
Supabase = قلب سیستم: Postgres، Edge Functions (Deno)، pg_cron + pg_net زمان‌بند، Realtime.

## قیدهای سخت — هرگز نقض نشود
1. Vercel Cron ممنوع (پلن Hobby فقط کران روزانه می‌دهد). همه زمان‌بندی‌ها pg_cron
   داخل Supabase که با pg_net به Edge Function ها HTTP می‌زند. کران‌ها UTC هستند؛
   بازار تهران شنبه‌تاچهارشنبه ۹:۰۰-۱۳:۰۰ تهران = ۵:۳۰-۹:۳۰ UTC.
2. RLS روی همه جدول‌ها فعال. نوشتن فقط با service_role (فقط در Edge Function / env).
   کلید anon فقط SELECT. service_role هرگز در کد فرانت یا git.
3. محاسبات اندیکاتور/متریک فقط روی کندل بسته‌شده (ضد repainting). منطق بک‌تست
   باید عیناً همان توابع lib/ منطق زنده را import کند — پیاده‌سازی موازی ممنوع.
4. آستانه‌های مبلغی (مثل پول درشت) پرسنتایلی، نه عدد ثابت تومانی (تورم).
5. هر collector: timeout + retry با backoff + ثبت وضعیت در pipeline_health.
   خطای یک منبع نباید بقیه را متوقف کند.
6. هر امتیاز/سیگنال باید تفکیک فاکتورهایش را ذخیره کند (jsonb reasons) — جعبه‌سیاه ممنوع.
7. LLM هرگز در مسیر صدور سیگنال نیست؛ فقط تفسیر زمینه (فاز ۶).
8. تاریخ در DB همیشه UTC/گرگوریان؛ در UI شمسی (date-fns-jalali).
9. در نمایش قیمت TSE همیشه هر دو: آخرین قیمت (pl) و قیمت پایانی (pc) — حجم مبنا
   این دو را جدا می‌کند.
10. commit های کوچک روی برنچ dev؛ merge به main فقط با تأیید کاربر. **ایمیل گیت روی این ریپو**
    (`git config user.email` محلی، نه global) عمداً `mohammadi.mani1231@gmail.com` است — همان
    ایمیل اکانت Vercel `mohammadimani1231-ai` که پروژهٔ `bazar-bourse` رویش است. پلن Hobby
    اجازهٔ عضو تیم نمی‌دهد، پس اگر author کامیت این ایمیل نباشد Vercel deploy را روی push خودکار
    block می‌کند («Git author ... must have access to the team»). این عمداً جدا از ایمیل global
    (`mohammadi.davoud1351@gmail.com`، مال ریپوهای دیگر مثل piro-admin) نگه داشته شده — دستکاریش نکن.
11. وضعیت بازار هرگز از حافظه یا فرمول «شنبه تا چهارشنبه» استنتاج نشود. تعطیلات بورس ایران
    شامل تعطیلات قمری متغیر و تعطیلی‌های اعلامی بی‌قاعده است. `lib/market-status.ts` منبع
    حقیقت است: اول جدول `market_holidays` (کمکی، کاربر پر می‌کند)، بعد اگر حجم نماد پرمعامله
    در ۱۵ دقیقهٔ اخیرِ بازهٔ نظری بازار تغییر نکرده باشد → بازار بسته فرض شود. هر Edge Function
    مربوط به بازار (collect-tse، health-check، compute-tabloo، compute-signals، build-candles،
    compute-rank) باید ابتدا این را چک کند؛ اگر بسته بود، ساکت با status='market_closed' در
    pipeline_health خارج شود، نه error. health-check هشدار «مرگ پایپ‌لاین» را فقط وقتی بازار
    باز است بدهد. برای Claude Code: هر جا به «باز بودن بازار» نیاز بود همین تابع/جدول را چک کن،
    هرگز از روز هفته یا دانش عمومی دربارهٔ تقویم ایران نتیجه‌گیری نکن.
12. **چارت‌ها و اعداد هرگز آینه نمی‌شوند** (بازطراحی ۲۰۲۶-۰۸-۰۵، `prompts/redesign-visual-language.md`).
    رابط RTL است (سایدبار راست، جهت خواندن راست‌به‌چپ) ولی محور زمان چارت‌ها همیشه چپ→راست
    می‌ماند (قدیمی‌ترین نقطه چپ، جدیدترین راست) و ارقام همیشه با `.ltr-nums` (که هم `direction:ltr`
    و هم `font-variant-numeric: tabular-nums` می‌دهد) رندر می‌شوند. lightweight-charts/ECharts
    کاملاً canvas/SVG-محورند و به `dir` صفحه کاری ندارند — هرگز تنظیمی برای معکوس‌کردنشان اضافه نکن.
13. **هر ویجت عددی باید برچسب as-of داشته باشد؛ نمایش بدون آن ممنوع.** دلیلش واقعی است، نه
    نظری: کارت «شاخص کل» نمای کلی ۲ روز کامل دادهٔ فریزشده نشان می‌داد بدون هیچ نشانه‌ای
    (۲۰۲۶-۰۸-۰۵، رجوع به بخش «اختلاف شاخص کل با TSETMC» پایین‌تر) — چون هیچ برچسب زمانی نداشت،
    staleness کاملاً نامرئی بود. از `components/AsOfBadge.tsx` استفاده کن (`capturedAt` +
    `marketOpen` از `lib/market-status.ts::isMarketOpen`، نه فرض جدید) — رنگ هشدار (`text-warning`)
    وقتی داده در ساعات بازار از `lib/market-status.ts::STALE_THRESHOLD_MS` (۱۵ دقیقه) قدیمی‌تر است.
14. **جریان دادهٔ پرتفوی مجازی همیشه یک‌طرفه است؛ هیچ حلقهٔ خودآموزی خودکار ساخته نمی‌شود.**
    مسیر مجاز فقط: موتور سیگنال ← موتور اجرای مجازی ← موتور محاسبهٔ عملکرد ← داشبورد/گزارش.
    هیچ خروجی پرتفوی مجازی (win rate، profit factor، برچسب علت، کارنامهٔ هر قانون) نباید
    به‌صورت خودکار وزن‌ها یا آستانه‌های `signal_rules` یا منطق `lib/signal-engine.ts` را تغییر
    دهد. تنها مسیر یادگیری، بازبینی انسان-در-حلقه است: گزارش فصلی فقط **پیشنهاد** تولید می‌کند
    و کاربر دستی تصمیم می‌گیرد. هر PR/تغییری که مسیر برگشتی خودکار بسازد نقض این قید است.
    نکتهٔ کارمزد (مستند تا در آینده باگ تلقی نشود): `scripts/backtest.ts::ROUND_TRIP_FEE` عمداً
    روی ۱.۵٪ مانده چون همهٔ گزارش‌های تاریخی بک‌تست با همان عدد تولید شده‌اند و تغییرش
    مقایسه‌پذیری را از بین می‌برد؛ پرتفوی مجازی با نرخ واقعی‌تر امروز (~۱.۷۵٪ = خرید ۰.۳۷٪ +
    فروش ۰.۸۸٪ + مالیات فروش ۰.۵٪) به‌صورت قابل‌تنظیم در `virtual_portfolio.fee_pct` کار می‌کند.

## منابع دیتا
- بورس ایران: BrsApi.ir (env: BRSAPI_KEY) — پایه `https://Api.BrsApi.ir` (از ۱۴۰۵/۰۲/۰۵ عوض شده، لینک قدیمی `BrsApi.ir/Api` از ۱۴۰۵/۰۵/۰۵ حذف می‌شود)
  - تک‌نماد: `/Tsetmc/Symbol.php?key=...&l18=<نماد فارسی>` — سهمیهٔ روزانه‌اش بسیار کم است، برای پایپ‌لاین استفاده نشود
  - همهٔ نمادها یک‌جا (برای collect-tse): `/Tsetmc/AllSymbols.php?key=...`
  - طلا/سکه/ارز (نرخ بازار آزاد، نه دولتی/نیمایی): `/Market/Gold_Currency.php?key=...`
- طلا/ارز ریالی: BrsApi (fallback: TGJU) — فقط نرخ بازار آزاد؛ نرخ دولتی/رسمی فعلاً خارج از scope است
- جهانی: Yahoo Finance غیررسمی https://query1.finance.yahoo.com/v8/finance/chart/{symbol}
  (BZ=F برنت، GC=F انس، HG=F مس، DX-Y.NYB، ^GSPC) — پشت adapter تا تعویض آسان باشد
- تاریخی TSE: اسکریپت seed یک‌بارمصرف Python با pytse-client (نیازمند Python ≤3.12 — lxml هنوز روی 3.14 wheel ندارد)
- تاریخی حقیقی/حقوقی (پیش‌نیاز متریک پول درشت): `/Tsetmc/History.php?key=...&type=1&l18=...` (تاریخ Jalali برمی‌گرداند).
- تاریخی دلار آزاد/طلای ۱۸ عیار (پیش‌نیاز بنچمارک بک‌تست): `/Market/Gold_Currency_Pro.php?key=...&section=currency|gold&history=2&symbol=USD|IR_GOLD_18K&date_start=...&date_end=...` (تاریخ Jalali، فرمت `date_end` باید `YYYY-MM-DD` امروزِ واقعی باشد نه تاریخ آینده وگرنه 400 می‌دهد).
- تاریخی شاخص کل (tedpix): `pytse_client.download_financial_indexes(symbols="شاخص كل")` (کاف/یای عربی، نه فارسی).
  ⚠️ هر دو endpoint بالای BrsApi (History.php و Gold_Currency_Pro.php) از IP بعضی محیط‌های ابری
  (مثل sandbox اجرای Claude Code) توسط BrsApi ریست می‌شوند — اگر backfill محلی fail شد، از
  Edge Function معادل (`backfill-buyer-breakdown`, `backfill-benchmarks` — هر دو از IP خود Supabase) استفاده کن.
  ⚠️ عکس این الگو هم دیده شده (۲۰۲۶-۰۸-۰۵، فاز ۱): `/Tsetmc/AllSymbols.php` که collect-tse زنده
  صدا می‌زند، از IP خود Supabase Edge Function گاهی چند تیک پیاپی کرون (هر ۲ دقیقه) روی تایم‌اوت
  شکست می‌خورد در حالی‌که از IPهای دیگر فوری جواب می‌دهد — یعنی این ناپایداری IP-محور مخصوص یک
  endpoint یا یک جهت نیست. **به‌روزرسانی ۲۰۲۶-۰۸-۰۵ (بعدتر همان روز): صرف افزایش timeout/retry
  کافی نبود** — بررسی `pipeline_health` واقعی نشان داد در دو روز معاملاتی پیاپی (۰۸-۰۴ و ۰۸-۰۵)
  نرخ خطای collect-tse حین بازار باز **~۹۸٪** بود. **فرضیهٔ «پروکسی از IP دیگر (Vercel) حل می‌کند»
  هم با تست زنده رد شد** — تماس مستقیم از Vercel هم با `ConnectTimeoutError` (نه پاسخ کند، اتصال
  اصلاً برقرار نمی‌شد) شکست خورد، یعنی این احتمالاً قطعی firewall/ASN-محور روی هر دو ابر است، نه
  چیزی که با تعویض IP ابری دور بزنی. `fetchAllSymbolsViaProxy` در کد ماند ولی رفع اصلی نیست.
  رفع واقعی فعلی: چون ~۲٪ اجراها همیشه سریع موفق می‌شدند (نه کند)، یعنی یک پنجرهٔ کوچک موفقیت
  واقعی هست — `fetchBrsApiAllSymbols` را از ۳ تلاش/۲۰ثانیه‌ای به **۷ تلاش/۸ثانیه‌ای** تغییر دادیم
  (بودجهٔ کل تقریباً یکسان، شانس برخورد با آن پنجره چند برابر) — **هنوز تأییدنشده که کافی است**،
  جزئیات کامل و صادقانه در بخش «وضعیت فازها» زیر عنوان incident.

## وضعیت فازها
- [x] فاز ۰: زیرساخت + تست روز صفر
- [x] فاز ۱: پایپ‌لاین دیتا
      - **اصلاح تکمیلی ۲۰۲۶-۰۸-۰۵: تاریخچهٔ روزانهٔ client-type (حقیقی/حقوقی) برای بک‌تست موجود
        است.** `scripts/seed_history.py` گسترش داده شد تا علاوه بر OHLCV، از همان pytse-client
        (`download_client_types_records`) ستون‌های `buy_i_volume`/`sell_i_volume`/`buy_count_i`/
        `sell_count_i` جدول `daily_candles` را برای کل بازهٔ ۵ سالهٔ هر ۴۴ نماد watchlist پر کند —
        قبلش این ستون‌ها فقط از `scripts/backfill_buyer_breakdown.py` (سقف ۱۳۰ روز، محدودیت
        BrsApi `History.php`) پر می‌شدند، یعنی برای ~۹۵٪ بازهٔ بک‌تست خالی بودند.
        **قوانین تابلوخوانی (buyer_power_strong، money_inflow_3d، suspicious_volume) باید در
        بک‌تست فاز ۳ لحاظ شوند** — این پیش‌فرض همیشه در `scripts/backtest.ts` برقرار بوده (خودش
        `lib/tabloo.ts` را روی همین ستون‌ها صدا می‌زند)، فقط تا امروز داده‌ای برای بیشتر بازه نداشت؛
        نتیجهٔ واقعی بعد از این بک‌فیل در چک‌لیست فاز ۳ ثبت شده.
        اجرای زنده idempotent: ۴۴ نماد، ۴۳٬۴۱۵ ردیف upsert‌شده، بازهٔ واقعی ۲۰۲۱-۰۸-۰۷ تا
        ۲۰۲۶-۰۸-۰۳، **پوشش ۱۰۰٪** (هر ۴ ستون در هر ردیف غیر-NULL — هیچ نقصی ثبت نشد).
      - **دو باگ واقعی حین این اصلاح کشف/رفع شد:** (۱) ستون‌های `buy_i_volume`/`sell_i_volume`
        در دیتابیس `bigint`اند، نه `numeric`؛ اولین نسخهٔ کد با `float()` مقداری مثل
        `"4202621718.0"` می‌ساخت که Postgres با خطای `22P02` رد می‌کرد — رفع با `int()`، مطابق
        همان الگویی که خود اسکریپت برای ستون `volume` قبلاً داشت. (۲) حلقهٔ OHLCV اصلی اسکریپت
        هیچ مصونیت خطای per-symbol نداشت — یک قطعی گذرای شبکهٔ `old.tsetmc.com` کل اجرا را روی
        نماد هفدهم متوقف کرد؛ رفع با try/except دورِ هر نماد (همان الگوی موجود در
        `backfill_buyer_breakdown.py`)، طوری‌که یک نماد ناموفق بقیه را متوقف نکند و اجرای بعدی
        idempotent جبرانش کند.
      - **حین همین بررسی، یک incident زندهٔ جداگانه هم کشف شد (رفع نشده، فقط تشخیص داده شد):**
        در حین باز بودن بازار امروز، `collect-tse` از ۰۶:۵۲ تا ۰۷:۰۸ UTC (۸ اجرای پیاپی کرون)
        با خطای `The signal has been aborted` (تایم‌اوت ۱۵ ثانیه‌ای `fetchBrsApiAllSymbols`)
        شکست خورد. تست مستقیم نشان داد خود endpoint BrsApi از این سندباکس فوری (۳۱۲ms) جواب
        می‌دهد — یعنی مشکل از IP خاص Supabase Edge Function است، شبیه الگوی شناخته‌شدهٔ ناپایداری
        BrsApi از IPهای ابری (قبلاً روی `History.php`/`Gold_Currency_Pro.php` مستند بود)، این‌بار
        روی `Tsetmc/AllSymbols.php` و در حین ساعات بازار. اثر جانبی: چون quote تازه نمی‌رسید،
        heuristic تشخیص باز/بستهٔ `lib/market-status.ts` (که به تغییر حجم نیاز دارد) بازار را
        غلط «بسته» تشخیص داد و `compute-tabloo`/`compute-signals`/`health-check` را هم متوقف نگه
        داشت. با فراخوانی دستی `collect-tse` سپس `compute-tabloo` بازیابی موقت انجام شد (برای
        اعتبارسنجی زیر).
        **رفع دائمی همان روز انجام و deploy شد:** `lib/data-sources/brsapi.ts`،
        `fetchBrsApiAllSymbols` از `{timeoutMs:15000}` (retry پیش‌فرض=۱) به
        `{timeoutMs:20000, retries:2, backoffMs:1000}` تغییر کرد (بدترین حالت ~۶۳ ثانیه، امن زیر
        بازهٔ کرون ۲ دقیقه‌ای). با `supabase functions deploy collect-tse` مستقر شد؛ اولین اجرای
        خودکار کرون بعد از deploy موفق بود (۳٫۹ ثانیه، سالم). **صادقانه:** چون خودِ قطعی BrsApi هم
        تقریباً هم‌زمان با deploy فروکش کرد، نمی‌شود با قطعیت گفت این تغییر همان incident را حل
        کرد یا خودش خودبه‌خود برطرف شد — ولی برای بلیپ‌های کوتاه‌تر مشابه در آینده قطعاً مقاوم‌تر است.
      - **اعتبارسنجی زندهٔ فرمول‌های تابلوخوانی (بخشی از آیتم باز فاز ۲، امروز):** با دادهٔ واقعاً
        زندهٔ فملی/وبملت (بعد از بازیابی بالا)، per_capita_buy/sell، buyer_power، money_flow
        دستی از روی فیلدهای خام `quotes` بازمحاسبه شد و **دقیقاً با مقدار ذخیره‌شده در
        `tabloo_metrics` یکی بود** — فرمول‌های `lib/tabloo.ts` روی دادهٔ زندهٔ واقعی تأیید شدند.
        `base_volume=1` برای هر دو نماد عجیب به نظر می‌رسید ولی چک شد: مستقیماً همان `bvol` خام
        BrsApi است، نه باگ ما.
- [x] فاز ۲: موتور تابلوخوانی — اعتبارسنجی زندهٔ فملی/وبملت با دادهٔ واقعی امروز (۲۰۲۶-۰۸-۰۵) انجام
      و تأیید شد (جزئیات در فاز ۱ بالا). **فولاد اصلاً در `watchlist` نبود** — این بخش از آیتم باز
      از اول قابل انجام نبوده، نه اینکه امروز هم رد شده باشد.
      **علت واقعی پیدا و مستند شد:** فولاد (فولاد مبارکه اصفهان) از ۲۰۲۶-۰۲-۲۵ در بورس متوقف است
      (آخرین کندل ثبت‌شده در pytse-client همان تاریخ؛ برای مقایسه فملی همان لحظه تا ۲۰۲۶-۰۸-۰۳
      داده داشت) و در دیتای زندهٔ امروز BrsApi هم اصلاً در فهرست معاملات نیست — با فرض کاربر
      (توقف نمادهای فولاد/پتروشیمی بعد از حملهٔ چند ماه قبل به این کارخانه‌ها) کاملاً هم‌خوان.
      به درخواست کاربر به `watchlist` اضافه شد (صنعت: «فلزات اساسی»، هم‌سطح با سیسکو/هرمز/ارفع/کاوه)
      و تاریخچهٔ کامل تا تاریخ توقف بک‌فیل شد (۱۰۳۶ کندل OHLCV + ۱۰۳۷ ردیف client-type، هر دو ۱۰۰٪
      کامل) تا وقتی نماد بازگشایی شد، پایپ‌لاین زنده (که فهرست را پویا از watchlist می‌خواند) بدون
      نیاز به تغییر کد خودش شروع به جمع‌آوری کند.
- [x] فاز ۳: موتور سیگنال + رتبه مرکب + بک‌تست — نتیجهٔ صادقانهٔ بک‌تست ۵ ساله (2021-01-01..2026-08-03):
      با قوانین اولیه ۶۵ معامله/بازده -۲۶٪؛ بعد از تنظیم وزن بر اساس همین بک‌تست، ۱۲ معامله/بازده +۰.۴۶٪.
      در هر دو حالت به‌شدت از buy-and-hold شاخص/دلار/طلا عقب‌تر است و نمونهٔ معاملات (۱۲-۶۵) برای
      نتیجه‌گیری آماری معتبر خیلی کوچک است — این سیستم فعلاً مدرکی برای alpha واقعی روی سهام ندارد.
      قبل از استفادهٔ واقعی، فاز‌های بعد باید این را جدی بگیرند، نه صرفاً چک‌لیست را تیک بزنند.
      - **به‌روزرسانی ۲۰۲۶-۰۸-۰۵ (بعد از بک‌فیل کامل ۵ سالهٔ client-type در فاز ۱ بالا):** همان
        بک‌تست، همان قوانین (بدون تغییر کد)، فقط با دادهٔ تابلوخوانی کامل به‌جای ~۱۳۰ روز آخر —
        **۱۳۳ معامله (به‌جای ۱۲)**، نرخ برد ۴۷.۴٪ (به‌جای ۶۶.۷٪، روی نمونهٔ به‌مراتب بزرگ‌تر و
        معتبرتر آماری)، profit factor ۱.۰۵۲ (عملاً بدون تغییر — یعنی هنوز فقط کمی سودده‌تر از
        زیان‌ده)، بازده کلی +۳.۹۵٪ (به‌جای +۰.۴۶٪)، ولی افت سرمایهٔ حداکثر هم عمیق‌تر شد: -۱۷.۵٪
        (به‌جای -۵.۳٪). قوانین تابلوخوانی که قبلاً تقریباً بی‌اثر بودند حالا واقعاً در بازی‌اند:
        `money_inflow_3d` از ۱۲ به ۱۲۱ بار trigger رسید، `buyer_power_strong` از ۹ به ۸۴،
        `suspicious_volume` از ۷ به ۴۲. **نتیجه هنوز به‌شدت از buy-and-hold عقب است**
        (تدپیکس +۲۷۵٪، دلار +۶۵۸٪، طلا +۱۵۶۷٪ در همین بازه) — این اصلاح فقط داده را کامل کرد،
        شواهد alpha واقعی را تغییر نداد. گزارش کامل قبل/بعد در `backtest-reports/` (gitignore).
      - ⚠️ **مبنای مقایسه از آن تاریخ دو بار جابه‌جا شده — عدد ۱.۰۵۲ بالا دیگر جاری نیست
        (۲۰۲۶-۰۸-۱۲):** (۱) اجرای ۰۸-۰۹ با **همان تاریخ پایان ۰۸-۰۵**، ۱۳۶ معامله و
        profit factor ۱.۰۴۹ داد، نه ۱۳۳/۱.۰۵۲ — یعنی خودِ `daily_candles` بین ۰۸-۰۵ و ۰۸-۰۹
        عوض شده (بک‌فیل/کندل‌های تازه)، نه اینکه کد تغییر کرده باشد. (۲) اجرای ۰۸-۱۱ با تاریخ
        پایان ۰۸-۱۱ (شش روز معاملاتی بیشتر): **۱۳۹ معامله، profit factor ۱.۰۶۴، بازده +۵.۰۰٪**
        — این عدد جاری است. نتیجه‌گیری کیفی عوض نشده (هنوز فقط کمی بالای ۱ و به‌شدت عقب‌تر از
        buy-and-hold)، ولی هر مقایسهٔ عددی آینده باید با ۱.۰۶۴ انجام شود نه ۱.۰۵۲.
        **این ربطی به refactor متریک‌ها (کامیت `71d058d`) ندارد** — آن refactor با اجرای مجدد
        روی همان تاریخ پایان ۰۸-۱۱ اثبات شد که هر ۱۹ عدد کلیدی را بدون تغییر نگه می‌دارد.
- [x] فاز ۴: داشبورد UI — هر ۶ صفحه (`/`, `/symbol/[symbol]`, `/signals`, `/screener`, `/global`, `/health`) ساخته
      و روی build/tsc/eslint/vitest (140 تست) تمیز رد شد. نکات مهم:
      - **دو باگ واقعی کشف و رفع شد حین ساخت:** (۱) همهٔ صفحات دیتای زنده بدون `export const dynamic =
        "force-dynamic"` به‌صورت static در build-time prerender و freeze می‌شدند — یعنی داشبورد deploy‌شده
        دیتای منجمد زمان build را تا deploy بعدی نشان می‌داد؛ اضافه شد به هر ۶ صفحه. (۲) PostgREST سقف
        پیش‌فرض ۱۰۰۰ ردیف دارد و `.limit()` بزرگ‌تر را ساکت نادیده می‌گیرد — کوئری چندنمادی/چنددارایی
        `/global` (order by date/captured_at بدون تفکیک asset) داشت فقط ۱۰۰۰ ردیف اول را برمی‌گرداند و
        نمودار rebase را خالی نشان می‌داد؛ با `fetchAllPages` (صفحه‌بندی `.range()`) در `app/global/page.tsx`
        رفع شد. این کلاس باگ (سقف ۱۰۰۰ ردیف Supabase) قبلاً هم در فاز ۳ کشف شده بود — یادآوری برای فازهای بعد.
      - **شکاف‌های اسکیما نسبت به پرامپت (گزارش‌شده و مستند، نه حدس زده):** نه «ارزش بازار» (مارکت‌کپ) و نه
        «شاخص هم‌وزن» در هیچ جدولی وجود ندارد — treemap/اسکرینر به‌جای مارکت‌کپ از ارزش معاملات لحظه‌ای
        استفاده می‌کنند (برچسب دقیق زده شده، تصمیم با کاربر گرفته شد)؛ شاخص هم‌وزن در UI با placeholder
        «دادهٔ موجود نیست» نشان داده می‌شود. «شاخص پالایشی» و «فلزات اساسی» برای پنل lead-lag داده نداشتند —
        با `lib/syntheticIndex.ts` (ترکیب هم‌وزن rebase-شدهٔ نمادهای همان صنعت در واچ‌لیست) جایگزین و برچسب‌گذاری
        شفاف شدند. برنت/انس‌طلا/DXY فقط از `global_quotes` (تازه فعال‌شده، همین چند روزه) می‌آیند نه یک جدول
        تاریخی — نمودار rebase/heatmap برایشان فعلاً کم‌داده است، ظرف چند هفته با جمع‌شدن دیتا کامل می‌شود.
      - `composite_rank` و `signals` هنوز خالی‌اند (اولین اجرای cron شبانه هنوز نیفتاده بود) — صفحات مربوطه
        empty-state درست نشان می‌دهند، رفتار مورد انتظار است نه باگ.
      - RSI۱۴/رتبهٔ مرکب/فاصله از MA۵۰ در اسکرینر به‌صورت زنده از `lib/composite-rank.ts` (همان تابع پایپ‌لاین
        زنده) روی `daily_candles` هر نماد محاسبه می‌شود، نه از جدول (که خالی بود) — طبق قاعدهٔ «بدون پیاده‌سازی موازی».
      - اعتبارسنجی زندهٔ فولاد/فملی/وبملت (فاز ۲) هنوز باز است — همچنان منتظر یک روز معاملاتی واقعی برای چک دستی.
- [x] فاز ۵: لایه ژئوپلیتیک + هشدار تلگرام — سه مکانیزم + هشدار ساخته و روی build/tsc/eslint/vitest
      (۱۷۹ تست) تمیز رد شد:
      - `compute-tension` (هر ۱۵ دقیقه، مستقل از ساعات بازار): z-score نوسان دلار + حباب سکه امامی +
        z-score تغییر برنت → gauge ۰-۱۰۰ در `global_quotes(asset='tension_index')`. sanity-check تاریخی
        واقعی نوشته شد (تست `lib/tension.test.ts`): بزرگ‌ترین جهش واقعی دلار در دادهٔ پروژه (۲۰۲۵-۰۶-۲۵،
        افت ۱۱.۳٪) z-score≈۵.۲ می‌دهد — رفتار درست و تأییدشده. **محدودیت شناخته‌شده:** مؤلفهٔ حباب سکه
        فعلاً عدد غیرمعقول بزرگ می‌دهد (gold_18k/coin_emami در برابر gold_ounce×usd_irr آربیتراژ نمی‌شوند)؛
        چون منشأش قطعی پیدا نشد (شاید عدم‌همزمانی snapshot تاریخی benchmark_candles با global_quotes زنده،
        نه لزوماً باگ)، gauge کلمپ‌شده امن است ولی مقدار مطلقش قابل‌اتکا نیست — قبل از استفادهٔ جدی از این
        مؤلفه باید دوباره بررسی شود.
      - **یک باگ واقعی کشف و رفع شد حین ساخت همین مؤلفه:** BrsApi `Gold_Currency.php` آرایهٔ `currency`
        (usd_irr) را به تومان می‌دهد ولی آرایهٔ `gold` (gold_18k/coin_emami) را به ریال — ناسازگاری واقعی
        در خود API، نه اشتباه ما. از فاز ۱ تا الان `global_quotes.usd_irr` ده برابر کوچک‌تر از
        `benchmark_candles.usd_irr` بود (کشف شد چون tension_index این دو را برای اولین بار مستقیم مقایسه
        کرد). رفع شد در `lib/transforms/globalQuote.ts` (×۱۰ فقط روی آرایهٔ currency) + مایگریشن
        `20260804160000` دادهٔ قبلی global_quotes را یک‌بار اصلاح کرد (idempotent با گارد schema_log).
        دیتای تیکر جهانی فاز ۴ («دلار آزاد») از همین تاریخ درست می‌شود — قبلش ۱۰ برابر کوچک‌تر نمایش داده می‌شد.
      - `collect-news` (ساعتی): فیدهای RSS واقعی تست‌شده زنده (دنیای اقتصاد، اقتصاد آنلاین، ایسنا، مهر،
        Tehran Times، Al Jazeera — همه در `settings.news_feeds`، configurable). پارسر RSS دست‌ساز
        (`lib/rss.ts`، بدون وابستگی خارجی) روی نمونهٔ واقعی تست شد. روی دادهٔ زندهٔ همین امروز ۳۷ خبر مرتبط
        (تنش هرمز، مذاکرات ایران-آمریکا، مصرف موشک...) گرفت و ذخیره کرد — کیفیت تطبیق کلیدواژه تأیید شد.
      - `settings(market_regime)` ساخته شد؛ سوییچ رژیم در نمای کلی + بنر در همهٔ صفحات (در root layout) کار
        می‌کند و زنده تست شد (روی مرورگر: سوییچ به تنش → بنر ظاهر شد → برگشت به عادی → بنر ناپدید شد).
        `compute-signals` دیگر رژیم را هاردکد `normal` نمی‌کند، از `settings` می‌خواند (آستانهٔ خرید ۶۰ در
        رژیم غیرعادی، طبق فاز ۳، از الان واقعاً وصل است).
      - `evaluate-alerts` (۱۰دقیقه‌ای، action: سیگنال جدید/جهش تنش/مرگ پایپ‌لاین) و `send-alert-digest`
        (ساعتی، info: حجم مشکوک/پول درشت/کد‌به‌کد — **همه در یک پیام**، طبق طراحی ضد نویز) دیپلوی و روی
        دادهٔ زنده تست شدند. secretهای تلگرام (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`/`SITE_URL`) با
        `supabase secrets set` ست شدند و **ارسال واقعی تأیید شد**: یک دایجست info از دادهٔ واقعی جلسهٔ
        معاملاتی ۱۴۰۵/۰۵/۱۳ (آریا/ارفع حجم مشکوک، آلومینا کد‌به‌کد) با `delivered=true` در `alert_log` رسید.
      - **باگ نویز کشف و رفع شد (مهم، چون کل ارزش ماژول هشدار به ضدنویز بودنش است):** سر هر ساعت که چند
        کرون هم‌زمان شلیک می‌کنند، `evaluate-alerts` گاهی با `PGRST303 JWT issued at future` (اسکیوی ساعت
        زیر بار، سمت زیرساخت Supabase) خطا می‌خورد؛ ۱۰ دقیقه بعد **خودش خطای خودش را می‌دید** و هشدار
        «مرگ پایپ‌لاین» می‌فرستاد — یک حلقهٔ خودارجاع که هر ساعت یک هشدار کاذب می‌ساخت. رفع: قانون
        `pipeline_health` حالا فقط سورس‌هایی را گزارش می‌کند که **آخرین وضعیتشان** خطاست (نه هر خطای گذرا
        در بازه)، و خود `evaluate-alerts` از این چک کنار گذاشته شد. بعد از رفع، اجرای دستی `fired: []` داد
        (درست — چون واقعاً هیچ سورسی خراب نیست).
      - `send-alert-digest` پارامتر اختیاری `{"sinceMinutes": N}` در بدنه می‌گیرد (فقط برای اجرای دستی،
        مثلاً دایجست کل یک جلسهٔ معاملاتی بعد از بسته‌شدن بازار)؛ کرون بدنه نمی‌فرستد و از watermark استفاده می‌کند.
      - **بخش تأییدنشدهٔ DoD:** هشدار **action** واقعی هنوز در تلگرام دریافت نشده — چون هیچ‌کدام از شرایطش
        الان صادق نیست (بازار بسته، سیگنال جدیدی نیست، تنش زیر آستانه، پایپ‌لاین سالم). مسیر ارسال دقیقاً
        همان `sendTelegramMessage` است که با دایجست تأیید شد، پس زیرساخت اثبات‌شده است؛ ولی اولین هشدار
        action واقعی باید در جلسهٔ معاملاتی بعدی رصد شود.
- [x] فاز ۶: لایه AI (بریف روزانه) — کد کامل، build/tsc/eslint/vitest (۱۹۷ تست) تمیز رد شد.
      **تنها بخش تأییدنشده: خود فراخوانی Claude API واقعی** — چون credit حساب Anthropic هنوز
      فعال نشده بود (خرید شد ولی activation یک روزه بود). کل پایپ‌لاین قبل از آن نقطه (ساخت
      ورودی ساخت‌یافته از ۷ بخش DB، فراخوانی، خطای واقعی ۴۰۰ credit، لاگ در pipeline_health)
      زنده تست و تأیید شد — یعنی تنها چیزی که آزمایش نشده خودِ پاسخ مدل و اعتبارسنجی zod روی
      یک پاسخ واقعی است (منطق پارس/zod با نمونهٔ دستی‌ساختهٔ دقیقاً هم‌فرمت prompt تست شده،
      نه واقعی از Claude). فردا با فعال‌شدن credit باید یک اجرای واقعی تأیید شود.
      - `daily-brief` (کرون `0 5 * * 6,0,1,2,3` = ۸:۳۰ تهران) ورودی را از ۷ بخش می‌سازد:
        global (برنت/انس/مس/DXY/S&P500 + تغییر ۲۴س/۷روزه از global_quotes)، domestic (دلار/طلای‌۱۸/حباب
        سکه از lib/tension.ts)، tension_index + market_regime، market (شاخص کل/هم‌وزن — حالا واقعی، طبق
        رفع‌شدن فاز ۴ — «ارزش معاملات خرد» چون تفکیک ندارد صریحاً به‌جایش «ارزش کل معاملات» فرستاده می‌شود)،
        signals (۲۴ساعت اخیر با reasons)، news (۲۴ساعت اخیر)، correlation_breaks (دلار×فلزات و برنت×پالایشی
        با `lib/correlationBreaks.ts` روی همان ترکیب هم‌وزن صنعتی فاز ۴).
      - اعتبارسنجی خروجی با zod (`lib/briefSchema.ts`) + یک retry خودکار + شکست نهایی در pipeline_health
        (نه insert جزئی/نامعتبر در ai_briefs).
      - System prompt عیناً طبق متن پرامپت فاز ۶، بدون تغییر.
      - **مشکل فنی کشف و رفع شد:** `lib/briefSchema.ts` (مشترک Next.js+Deno) با `import { z } from "zod"`
        در Deno با خطای «Relative import path not prefixed with /» شکست می‌خورد چون Deno بدون import map
        بارهٔ package name را نمی‌شناسد. رفع: `supabase/functions/deno.json` با نگاشت `"zod":"npm:zod@^4"` +
        `[functions.daily-brief] import_map=...` در `config.toml` (خودکار روی هر deploy بعدی اعمال می‌شود،
        نیازی به `--import-map` دستی نیست).
      - UI: کارت بریف در نمای کلی (mood + summary + ریسک اصلی، خلاصه) پر شد (placeholder فاز ۴ حذف شد)،
        صفحهٔ `/briefs` برای آرشیو کامل با برچسب اطمینان رنگی و tooltip قابل‌بازشدن روی هر `ref` (نمایش خام
        همان بخش input_snapshot که ادعا به آن ارجاع داده — بر اساس بخش سطح‌بالا، نه مسیر دقیق، چون فرمت آزاد
        `ref` را نمی‌شود کاملاً پیش‌بینی کرد). دکمهٔ «توضیح بده» در `/signals` بدون LLM کار می‌کند
        (`lib/signalExplain.ts`، template متنی از reasons).
- [x] فاز ۷: ماژول گزارش‌گیری تحلیلی — کد کامل، build/tsc/eslint/vitest (۲۲۱ تست) تمیز رد شد.
      جدول `reports (type, period, html, data_snapshot, created_at)` + RLS select-عمومی. گزارش‌ها
      HTML ثابتِ سمت‌سرور هستند (بدون وابستگی JS کلاینتی، طبق طراحی «قابل چاپ به PDF»):
      `lib/reportCharts.ts` (SVG bar/line دست‌ساز) + `lib/reportHtml.ts` (قالب جدول/بخش/شِل با
      `@media print` که فقط `.report-root` را چاپ می‌کند) + `lib/jalaliMonth.ts`/`lib/seasonality.ts`
      (فصلی‌نگری ماه شمسی) + `lib/weeklyBriefSchema.ts` (zod خلاصهٔ اجرایی، `ConfidenceLevel` مشترک
      با `lib/briefSchema.ts` فاز ۶).
      - **گزارش هفتگی** (`weekly-report`، کرون `30 6 * * 4` = ۱۰:۰۰ پنجشنبهٔ تهران): هر ۸ بخش
        (خلاصهٔ اجرایی LLM، بازار در یک نگاه، جریان پول، زمینهٔ جهانی، رژیم و تنش، کارنامهٔ موتور
        سیگنال، هفتهٔ پیش رو، رتبه‌بندی واچ‌لیست) با تجدید استفادهٔ کامل از توابع پایپ‌لاین زنده
        (`ruleStats`، `composite-rank`، `correlationBreaks`، `syntheticIndex`، `downsampleDaily`،
        `tension`) طبق قاعدهٔ «بدون پیاده‌سازی موازی». پیام تلگرام «گزارش هفتگی جدید» خارج از دایجست،
        طبق طراحی.
      - **مشکل فنی کشف و رفع شد:** `date-fns-jalali` (وارد شده از `lib/jalali.ts`) در Deno با همان
        خطای «package name نامعتبر» زود که `zod` در فاز ۶ داشت شکست می‌خورد؛ نسخهٔ نصب‌شده
        پیش‌انتشار است (`4.4.0-0`) و semver `^4` روی prerelease مچ نمی‌شود — رفع با پین دقیق
        `"date-fns-jalali": "npm:date-fns-jalali@4.4.0-0"` در `supabase/functions/deno.json`.
      - **باگ واقعی کشف و رفع شد:** وقتی `usd_irr`/`coin_emami` در طول هفته فقط یک رکورد `global_quotes`
        داشتند (وقفهٔ شناخته‌شدهٔ BrsApi از فاز ۵)، بخش «زمینهٔ جهانی» عدد گمراه‌کنندهٔ «۰٫۰٪» نشان
        می‌داد (چون اولین و آخرین رکورد هفته یکی بودند). رفع: گارد `weekRows.length < 2` که به‌جایش
        «—» نشان می‌دهد. با اجرای مجدد زندهٔ تابع تأیید شد (گزارش id=5، ستون دلار آزاد/سکه امامی
        درست «—» شد). **علت ریشه‌ای (خرابی متناوب endpoint طلا/ارز BrsApi در `collect-global`) هنوز
        رفع نشده** — این فقط لایهٔ نمایش را اصلاح کرد، نه پایپ‌لاین جمع‌آوری را.
      - یک گلیچ گذرای پلتفرم Supabase Edge (اولین اجرا، ستون شاخص کل خالی برگشت با اینکه کوئری و
        دیتا هر دو سالم بودند) در اجرای دوم رفع شد بدون تغییر کد — همان الگوی شناخته‌شدهٔ ناپایداری
        از فاز ۵ (`evaluate-alerts`/`JWT issued at future`)، تکرار نشد.
      - **گزارش عمیق تک‌نماد** (`generateSymbolReport` در `app/symbol/[symbol]/actions.ts`، Server
        Action): همان ۸ بخش پرامپت (پروفایل، عملکرد قیمت adjusted + نمای دلاری + مقایسه با شاخص
        صنعت/کل، تابلوخوانی ۳۰روزه، رفتار صف، تاریخچهٔ سیگنال، همبستگی با برنت/دلار/انس با CCF و لگ
        بهینه از `lib/stats.ts`، فصلی‌نگری، جمع‌بندی LLM با جملهٔ ثابت پایانی). دکمهٔ «تولید گزارش
        عمیق نماد» در صفحهٔ نماد. **تست زنده روی دو نماد واقعی:** خودرو (پرمعامله — همبستگی و
        فصلی‌نگری واقعی محاسبه شد) و آلومینا (کم‌داده، ۲۷۴ کندل — بدون کرش، همه‌جا «داده کافی نیست»/
        «داده‌ای برای این بخش نیست» درست نمایش داده شد).
      - صفحهٔ `/reports`: آرشیو با فیلتر نوع + جستجوی نماد (`ilike` روی `period`) در نمای فهرست،
        نمای تک‌گزارش با دکمهٔ چاپ (`window.print()`، همان print-stylesheet تعبیه‌شده در HTML ذخیره‌شده).
        لینک «گزارش‌ها» به `Nav.tsx` اضافه شد.
      - **سناریوی بدون LLM طبیعتاً روی هر دو نوع گزارش تست شد:** چون `ANTHROPIC_API_KEY` هنوز نه در
        Vercel env و نه فعال روی حساب Anthropic است، هم گزارش هفتگی و هم گزارش‌های تک‌نماد بدون خلاصهٔ
        هوش‌مصنوعی و با پاراگراف fallback مشخص تولید شدند — بقیهٔ گزارش کامل و سالم ماند.
      - **بخش تأییدنشدهٔ DoD:** تحویل واقعی پیام تلگرام «گزارش هفتگی جدید» در چت کاربر مستقیماً چک
        نشد (این مسیر خارج از `alert_log` است، پس رکورد دیتابیسی هم ندارد که بشود چکش کرد).
      - **کار باقی‌مانده برای بعد:** افزودن `ANTHROPIC_API_KEY` به Vercel env (فعلاً فقط در Supabase
        secrets است، که برای Edge Functionها کافی است ولی گزارش تک‌نماد چون از سمت Next.js/Vercel
        فراخوانی می‌شود کلید جدا لازم دارد).
- [x] فاز ۸ (۲۰۲۶-۰۸-۰۵): مدیریت ریسک و اندازه پوزیشن — این فاز خارج از نقشهٔ اصلی فازهای ۰-۱۰
      بود (پرامپتش جدا از `docs/prompts/`، مستقیم توسط کاربر داده شد). کد کامل، build/tsc/eslint/
      vitest (۲۴۴ تست) تمیز رد شد. مایگریشن `20260805140000_stage08_risk_management.sql` هنوز
      روی production اجرا نشده — منتظر تأیید کاربر (رجوع به بخش deploy زیر).
      - **اصل طراحی:** این ماژول تصمیم نمی‌گیرد، محدود می‌کند — بدون اجرای خودکار معامله، فقط
        محاسبه و هشدار.
      - **تصمیم مهم مکتوب (نه حدس):** پرامپت فاز ۸ گفته بود `calculatePositionSize` به
        `stopLossPrice` نیاز دارد ولی نگفت این عدد از کجا بیاید — و در کل کدبیس هیچ مفهوم حد
        ضرر وجود نداشت. از کاربر پرسیده شد (۳ گزینه: ورودی دستی/ATR/کف نوسانی)؛ کاربر چون
        تازه‌کار بود تصمیم را به Claude Code واگذار کرد. تصمیم: **ATR14×1.5** (رایج، قابل توضیح،
        data-driven به‌جای عدد ثابت) — `lib/indicators.ts::atr()` (اضافه شد، الگوی هموارسازی
        Wilder دقیقاً هم‌ساز با `rsi()` موجود، مقادیر مرجع مستقل با pandas تولید و تست شدند) +
        `lib/position-sizing.ts::suggestStopLossFromAtr()`. فقط پیش‌فرض صفحهٔ سیگنال‌هاست، کاربر
        می‌تواند در فرم ثبت دستی معامله (`/portfolio`) جایگزینش کند.
      - **پیامد طراحی مهم دیگر:** جدول `paper_trades` (طبق پرامپت) ستون جهت ندارد — یعنی
        long-only. پس پیشنهاد اندازه پوزیشن در صفحهٔ سیگنال‌ها **فقط برای `direction=buy`**
        نمایش داده می‌شود، نه برای سیگنال فروش.
      - `lib/tabloo.ts::queueState` گسترش یافت: `lockedSell` (offer روی کف مجاز) کنار
        `lockedBuy` موجود اضافه شد — قبلاً فقط قفل صف خرید بود، ولی هشدار حد ضرر دقیقاً به قفل
        صف **فروش** نیاز دارد (نمی‌شود در صف فروخت). در `compute-tabloo` هم persist می‌شود
        (`queue_locked_sell`) و در `QueueFlags` صفحهٔ نماد هم بج جدید نمایش داده می‌شود.
      - `lib/position-sizing.ts::calculatePositionSize`: ریسک مجاز = سرمایه×درصد×ضریب رژیم؛
        سه هشدار مجزا (نه یکی کلی): عبور از سقف پوزیشن (محدود می‌کند، رد نمی‌کند)، حد ضرر خارج
        از دامنهٔ نوسان مجاز یک جلسه (±۵٪ پیش‌فرض، از `price_max`/`price_min` واقعی نه فرض ثابت
        در جاهایی که در دسترس است)، قفل صف خرید/فروش (از `queueState` زندهٔ لحظه).
      - `lib/portfolioLimits.ts::checkPortfolioLimits`: فقط سقف تعداد پوزیشن هم‌زمان «سخت»
        (blocked) است؛ تمرکز صنعتی و همبستگی (با `pearsonCorrelation` موجود در `lib/stats.ts`،
        بدون پیاده‌سازی موازی) فقط هشدار می‌دهند.
      - صفحهٔ `/settings/risk` (فرم ساده) و `/portfolio` (پوزیشن‌های باز با P&L واقع‌نشدهٔ زنده از
        آخرین quote، معاملات بسته‌شده با P&L محقق‌شده، تخصیص صنعتی، ثبت/بستن معامله دستی).
      - **برچسب صادقانه:** «equity curve» واقعی (مارک‌تومارکت پیوسته) نیاز به snapshot دوره‌ای
        دارد که این فاز نساخت (خارج از scope پرامپت). به‌جایش «منحنی سود/زیان محقق‌شدهٔ تجمعی»
        (فقط از معاملات بسته‌شده) ساخته و **صریح همین‌طور لیبل شد** تا گمراه‌کننده نباشد؛ از
        `lib/reportCharts.ts::renderLineChartSvg` موجود (فاز ۷) استفاده شد، نه چارت جدید.
      - پیشنهاد اندازه پوزیشن در صفحهٔ سیگنال‌ها (`components/PositionSizeSuggestion.tsx`) فقط
        وقتی ردیف باز می‌شود محاسبه می‌شود (نه eager روی هر ۱۵۰ سیگنال) — با کلاینت anon سمت
        مرورگر (قید ۲: anon فقط SELECT، همان الگوی Realtime موجود در `SignalsTable`).
      - **کار باقی‌ماندهٔ شناخته‌شده:** مایگریشن هنوز اجرا نشده (منتظر تأیید)؛ بعد از اجرا، باید
        یک بار زنده با یک نماد واقعی تست شود (فعلاً فقط unit test، نه end-to-end با دیتای واقعی).
(در پایان هر فاز این چک‌لیست را آپدیت کن.)

## بازنگری زبان بصری (در جریان، برنچ `feat/redesign-visual-language`)

فازهای ۰-۱۰ کامل‌اند؛ این یک بازسازی بصری کل محصول، جدا از نقشهٔ فازها، بر اساس بریف
`prompts/redesign-visual-language.md` (پنج مرجع: TradingView/Koyfin/Finviz/Robinhood/
Yahoo Finance). ترتیب: اول پایهٔ طراحی مشترک، بعد صفحه‌به‌صفحه.

- [x] **پایهٔ طراحی (۲۰۲۶-۰۸-۰۵)**: پالت رنگ به خاکستری تیرهٔ آبی‌مایل تغییر کرد (نه سیاه
      مطلق — `--background` از `#0a0a0c` به `#12141c`)، `.ltr-nums` علاوه بر `direction:ltr`
      حالا `font-variant-numeric: tabular-nums` هم دارد. `Nav.tsx` (نوار بالا) حذف و با
      `Sidebar.tsx` (سایدبار راست، آیکون `lucide-react`، icon-only زیر `lg`) جایگزین شد؛
      `app/layout.tsx` به `flex` افقی تغییر کرد. سه primitive جدید ساخته شد:
      `Sparkline` (SVG خالص، رنگ از روی علامت بازدهٔ کل بازه)، `EmptyState`، `PriceFlash`
      (فلش رنگی ۶۰۰ms روی تغییر مقدار، نه چشمک‌زن دائمی — روی `PriceHeader` وصل شد به‌عنوان
      اثبات مفهوم). قاعدهٔ سخت جدید در بند قیدها (#۱۲): چارت و اعداد هرگز آینه نمی‌شوند —
      با `agent-browser` زنده تأیید شد (کندل‌چارت فملی: قدیمی‌ترین کندل چپ، جدیدترین راست،
      دقیقاً مثل TradingView، با اینکه صفحه RTL است).
      build/tsc/eslint/vitest (۲۲۱ تست) تمیز رد شد.
      **یادداشت مهم از اسکرین‌شات‌های واقعی کاربر** (نه فقط توصیف) در همان فایل پرامپت ثبت
      شده — برای فاز بعد: نام شرکت زیر نماد در جدول (نه فقط ستون جدا، الگوی Koyfin)، treemap
      باید به‌تفکیک صنعت گروه‌بندی و لیبل مستقیم روی باکس‌های بزرگ بگذارد (الگوی Finviz).
- [x] **صفحه‌به‌صفحه (۲۰۲۶-۰۸-۰۵)**، هر سه تأیید بصری زنده با `agent-browser`:
      - **اسکرینر (Finviz)**: `RangeSliderFilter` (اسلایدر دوطرفه، یک دسته‌بندی هم‌زمان دیده
        می‌شد) حذف و با `FilterDropdown` جدید (بازه‌های از‌پیش‌تعیین‌شده در select فشرده)
        جایگزین شد؛ منطق فیلتر زیرین (`lib/screenerFilters.ts`) دست‌نخورده ماند. روی هر تب
        badge تعداد فیلتر فعال اضافه شد (چون فیلتر یک تب هنگام دیدن تب دیگر مخفی می‌ماند).
        نام شرکت از ستون جدا به زیر نماد در همان سلول منتقل شد (الگوی واقعی Koyfin).
        زنده تست شد: فیلتر «اشباع خرید (بالای ۷۰)» درست ۴۵ ردیف را به ۹ کاهش داد.
      - **نمای کلی (Koyfin + Finviz)**: `MarketTreemap` از لیست تخت به ساختار تودرتوی
        صنعت→نماد تغییر کرد (هر صنعت یک خوشهٔ لیبل‌دار، رنگ فقط روی برگ‌ها) + لیبل مستقیم
        نماد+عدد روی خود باکس (نه فقط هاور) — دقیقاً الگوی نقشهٔ S&P 500 در اسکرین‌شات واقعی
        Finviz که کاربر فرستاد. اعداد `IndexSummary` از `text-lg` به `text-2xl` با درصد تغییر
        هم‌ردیف بزرگ شدند (سلسله‌مراتب تایپوگرافی Koyfin).
      - **صفحهٔ نماد (Yahoo Finance + TradingView)**: `PriceHeader` چسبان به بالای صفحه شد
        («قیمت همیشه جلوی چشم»)؛ آخرین قیمت (pl) قیمت اصلی بزرگ با درصد تغییر روزانه (محاسبه‌شده
        از آخرین کندل بستهٔ واقعی، نه دادهٔ ساختگی) کنارش، قیمت پایانی (pc) به‌عنوان آمار ثانویه
        — **هر دو طبق قید #۹ همیشه نمایش داده می‌شوند**. یک ردیف آمار دوستونه (قیمت پایانی روز
        قبل + سقف/کف مجاز امروز) اضافه شد — برچسب عمداً «سقف/کف مجاز» است نه «بازهٔ روز»، چون
        `price_max`/`price_min` طبق کامنت خود `lib/transforms/quote.ts` سقف/کف مجاز معاملاتی‌اند
        نه بیشینه/کمینهٔ واقعی قیمت روز. محتوای صفحه به سه تب (چارت/تابلوخوانی/سیگنال‌ها، الگوی
        `SignalsTabs` موجود) تقسیم شد؛ حالت خالی هرکدام از `EmptyState` استفاده می‌کند.
      build/tsc/eslint/vitest (۲۲۱ تست) بعد از هر سه صفحه تمیز رد شد.

## گیت رمز مشترک کل سایت (`feat/site-password-gate`، ۲۰۲۶-۰۸-۰۵)

قبل از این، سایت **هیچ سیستم auth نداشت** — با اینکه پروژه تک‌کاربره/شخصی طراحی شده بود، هر
جدولی RLS با `select using (true)` دارد (عمدی، برای کلید anon) و مهم‌تر: چند Server Action
نوشتنی (`addToWatchlist`، `savePreset`، سوییچ `market_regime` در `app/actions/settings.ts`،
و در آینده `generateSymbolReport` که هزینهٔ API دارد) بدون هیچ محافظتی در دسترس هرکسی با لینک
بودند. کاربر خواست بتواند لینک را با یک دوست برای تست به‌اشتراک بگذارد، پس:

- `proxy.ts` (نه middleware — طبق تغییر Next.js 16) کل سایت را می‌بندد، فقط `/login` باز است.
  **قید مهم مستندشده در proxy.ts:** طبق خود مستندات Next.js 16 (`node_modules/next/dist/docs/
  .../proxy.md`)، Server Function ها مسیر جدا نیستند و با matcher ناقص می‌شود بی‌خبر رد شوند —
  matcher عمداً همه‌چیز به‌جز `_next/static`/`_next/image`/`favicon.ico` را می‌گیرد.
- `app/login` + `app/login/actions.ts`: فرم رمز ساده (بدون سیستم کاربر واقعی). کوکی مقدارش
  **هش SHA-256 رمز است، نه خود رمز** (`lib/siteAuth.ts`، منبع واحد برای proxy و login).
  fail-closed: اگر `SITE_PASSWORD` ست نشده باشد، همه رد می‌شوند (نه این‌که همه رد بشن باز بمونه).
- **محدودیت شناخته‌شده (پذیرفته‌شده، نه باگ):** چون `/login` هنوز از `app/layout.tsx` اصلی رد
  می‌شود، سایدبار و بنر رژیم پشت فرم لاگین دیده می‌شوند (لینک‌ها کار نمی‌کنند، فقط ظاهر). رفعش
  نیاز به route group جدا و جابه‌جایی کل صفحات دارد — برای این حجم کار (بازدید یک دوست) لازم
  تشخیص داده نشد؛ اگر بعداً خواستی درستش کنیم بگو.
- `SITE_PASSWORD` هم در `.env.local` (تست محلی) و هم در Vercel (Production + Preview، از قبل
  با `vercel env add` ست شده) — اگر عوضش کردی هر دو جا باید هماهنگ بمونه.
- **تست زنده با agent-browser:** رمز غلط → پیام خطا، رمز درست → ریدایرکت به مسیر مقصد + ماندن
  session روی صفحات دیگر بدون لاگین دوباره. یک نکتهٔ اجرایی حین تست: dev server باید بعد از
  اضافه‌کردن env var جدید ری‌استارت شود — Next.js آن را در پردازش زندهٔ قبلی هات‌ریلود نمی‌کند.

## سه incident واقعی از صفحهٔ سلامت (۲۰۲۶-۰۸-۰۵، کشف‌شده توسط کاربر)

کاربر مستقیماً از `/health` سه خطای پیاپی گزارش داد. بررسی `pipeline_health` واقعی (کوئری مستقیم
REST، نه حدس) این سه را تأیید/رد کرد:

1. **`collect-tse` — «The signal has been aborted» — جدی‌ترین، تا حدی بهتر شد، هنوز کاملاً رفع نشده — صادقانه.**
   کوئری واقعی: نرخ خطا در ۳۰۶ رکورد اخیر **۲۵۵ خطا / ۴۵ market_closed / فقط ۶ ok** — یعنی
   دو روز معاملاتی پیاپی (۰۸-۰۴، ۰۸-۰۵) عملاً **~۹۸٪ شکست** حین بازار باز. رفع اول همان روز
   (افزایش timeout/retry) کافی نبود.
   **تلاش دوم (پروکسی Vercel) هم با تست زنده رد شد:** فرضیه این بود که IP/ASN جدای Vercel این
   قطعی IP-محور را دور می‌زند. بعد از deploy، تماس مستقیم از Vercel هم با
   `ConnectTimeoutError: Connect Timeout Error (attempted address: api.brsapi.ir:443, timeout: 10000ms)`
   شکست خورد — یعنی این احتمالاً یک قطعی firewall/ASN-محور روی هر دو ابر (Supabase و Vercel)
   است، نه چیزی که با تعویض IP ابری قابل دورزدن باشد. `fetchAllSymbolsViaProxy`/Route Handler
   `app/api/internal/brsapi-all-symbols` در کد ماند (بی‌ضرر، ممکن است روزی مفید شود) ولی
   **در عمل رفع اصلی نیست.**
   **رفع واقعی اعمال‌شده (مبتنی بر شواهد، نه فرض):** چون ۶ اجرای موفق از ۳۰۶ (~۲٪) همیشه سریع
   بودند (۳-۴ ثانیه، نه نزدیک مرز timeout)، یعنی قطعی مطلق نیست — یک پنجرهٔ کوچک موفقیت هست.
   `fetchBrsApiAllSymbols` از ۳ تلاش با timeout ۲۰ ثانیه (بدترین حالت ~۶۳ ثانیه) به **۷ تلاش با
   timeout ۸ ثانیه** (بدترین حالت مشابه ~۶۶ ثانیه، هنوز امن زیر بازهٔ کرون ۲ دقیقه‌ای) تغییر
   کرد — timeout کوتاه‌تر چون وقتی واقعاً جواب می‌دهد سریع است، و تعداد تلاش بیشتر یعنی شانس
   بیشتر برای برخورد با آن پنجرهٔ کوچک در همان بازهٔ زمانی.
   **هنوز تأییدنشده:** آیا این واقعاً نرخ موفقیت را از ~۲٪ بالاتر می‌برد یا نه — فقط بعد از چند
   روز معاملاتی واقعی با کوئری مجدد `pipeline_health` قابل تأیید است، الان فقط یک فرضیهٔ
   موجه‌تر است، نه یک «رفع تضمین‌شده». اگر این هم کافی نبود، گزینه‌های باقی‌مانده جدی‌ترند
   (مثلاً پروکسی/VPN با IP غیرابری) و باید با کاربر دربارهٔ هزینه/پیچیدگی صحبت شود.
   `BRSAPI_KEY`/`BRSAPI_PROXY_SECRET` در Vercel ست و همهٔ توابع مرتبط deploy شدند (۲۰۲۶-۰۸-۰۵).

2. **`daily-brief` — «Your credit balance is too low» — قابل رفع با کد نیست، اکشن کاربر لازم است.**
   کوئری واقعی سه بار متوالی (۰۸-۰۴ ۲۰:۲۹، ۰۸-۰۴ ۲۱:۰۴، ۰۸-۰۵ ۰۵:۰۰) همان خطای ۴۰۰ Anthropic
   billing را نشان داد. طبق چک‌لیست فاز ۶ بالا، خرید credit روز ۰۸-۰۴ انجام شده بود ولی
   activation «یک روزه» اعلام شده بود؛ این خطاها **بعد از** آن پنجرهٔ یک‌روزه هم ادامه دارند —
   یعنی یا خریدِ credit روی حساب/کلید دیگری بوده (نه همان که در Supabase secrets به‌عنوان
   `ANTHROPIC_API_KEY` ست شده)، یا فعال نشده، یا مصرف شده. **این یک تصمیم/اکشن مالی است، Claude
   Code به Console Anthropic دسترسی ندارد** — کاربر باید در
   `console.anthropic.com` → Billing بررسی کند که (الف) پرداخت واقعاً روی حساب درست ثبت شده،
   (ب) کلیدی که در Supabase secrets هست دقیقاً متعلق به همان حساب/workspace است. کد فعلی از قبل
   درست رفتار می‌کند: به‌جای هدر رفتن به retry بی‌فایده، همان تلاش اول خطای واقعی را در
   `pipeline_health` ثبت می‌کند و می‌ایستد — چیزی برای «رفع» در کد نبود.
   یادآوری جدا (فاز ۷): این کلید هنوز در **Vercel هم ست نشده** (فقط Supabase) — برای گزارش عمیق
   تک‌نماد لازم است، کار باقی‌ماندهٔ شناخته‌شده، ربطی به این باگ ندارد.

3. **`evaluate-alerts` — «JWT issued at future» — قبلاً تشخیص داده شده بود، امروز تقویت شد.**
   قبلاً در فاز ۵ این incident کامل مستند و علتش («اسکیوی ساعت زیر بار Supabase وقتی چند کرون
   هم‌زمان شلیک می‌کنند») پیدا شده بود، و مکانیزم ضدنویز `pipeline_health` (فقط آخرین وضعیت هر
   سورس، نه هر خطای گذرا) از هشدار کاذب جلوگیری می‌کرد — کوئری واقعی تأیید کرد: فقط ۵ بار در دو
   روز (۱۳:۰۰، ۱۹:۳۰، ۱۸:۰۰، ۱۷:۰۰، ۱۴:۰۳)، هیچ‌کدام به هشدار کاذب «مرگ پایپ‌لاین» منجر نشد. تنها
   اثر باقی‌مانده: خودِ آن اجرای ۱۰دقیقه‌ای evaluate-alerts (نه بقیهٔ پایپ‌لاین) آن یک چرخه را رد
   می‌کرد چون تنها کوئری اول (`alert_rules`) با `if (rulesError) throw` واقعاً fail می‌شد. رفع
   امروز: `withJwtSkewRetry` در `evaluate-alerts/index.ts` — همان کوئری را بعد از ۱٫۵ ثانیه تأخیر
   یک‌بار دیگر امتحان می‌کند (فقط روی کد خطای دقیق `PGRST303`، نه هر خطای دیگر) قبل از اینکه کل
   چرخه را fail کند.

## اختلاف شاخص کل با TSETMC + نبود برچسب زمانی (۲۰۲۶-۰۸-۰۵)

کاربر با مقایسهٔ دستی دو کارت را گزارش داد: شاخص کل ۲.۴٪ و ارزش معاملات ~۱۰٪ با tsetmc.com
اختلاف داشت، بدون هیچ برچسب «به‌روزرسانی» روی هیچ‌کدام از سه کارت نمای کلی. تشخیص قبل از اصلاح
انجام و توسط کاربر تأیید شد — نتیجه با فرضیهٔ اولیهٔ خودِ گزارش (که همه‌چیز را به وقفهٔ
`collect-tse` نسبت می‌داد) فرق داشت:

1. **شاخص کل/هم‌وزن اصلاً به `collect-tse` مربوط نبود.** `benchmark_candles(asset='tedpix')`
   هیچ نویسندهٔ زنده‌ای نداشت — تنها منبعش `scripts/backfill_benchmarks.py` بود، یک اسکریپت
   پایتون **یک‌بارمصرف و محلی** (نه cron، نه Edge Function) که کامنت خودش هم صریح می‌گفت. کوئری
   واقعی: آخرین رکورد تاریخ ۲۰۲۶-۰۸-۰۳ داشت (۲ روز معاملاتی جا افتاده) و تا اجرای دستی بعدی
   هرگز خودش تازه نمی‌شد — مستقل از سلامت هر پایپ‌لاین دیگری.
2. **ارزش معاملات دو علت مستقل داشت:** (الف) از `quotes` زنده می‌آمد، پس واقعاً وقفهٔ
   `collect-tse` تأثیر داشت؛ (ب) فقط مجموع ۴۵ نماد واچ‌لیست بود نه کل بازار — مستقل از
   staleness و با رفع `collect-tse` هم صفر نمی‌شد.
3. **کشف کلیدی حین بررسی راه‌حل:** `Tsetmc/Index.php?type=1` در BrsApi (بدون مستندات رسمی،
   با تست زنده کشف و تأیید شد — `index` دقیقاً با عدد tsetmc.com یکی بود) یک خلاصهٔ زندهٔ کامل
   بازار می‌دهد: `index`/`index_change` (شاخص کل)، `index_equalWeight` (هم‌وزن)، `tval` (ارزش
   کل معاملات **واقعی بازار**، نه فقط واچ‌لیست)، `mv` (ارزش کل بازار — چیزی که فاز ۴ اصلاً
   نداشت)، `state` (باز/بسته). `type=2` فرابورس است، `type=3` فهرست تفکیکی زیرشاخص‌ها —
   فعلاً استفاده نشدند.

**رفع نهایی (با تأیید کاربر برای هر دو تصمیم):**
- `components/AsOfBadge.tsx` (کامپوننت مشترک، طبق قید #۱۳ بالا) روی هر سه کارت `IndexSummary`
  نصب شد؛ `lib/market-status.ts::isStaleAsOf` (تابع خالص، تست‌شده) قاعدهٔ رنگ هشدار را از همان
  `isMarketOpen`/`STALE_THRESHOLD_MS` موجود می‌گیرد، نه منطق جدید.
- **کالکتور زندهٔ جدید** به‌جای صرفاً اصلاح برچسب: جدول `market_index_quotes` (مایگریشن
  `20260805150000_market_index_live.sql`) + Edge Function `collect-market-index` (همان الگو و
  بازهٔ `collect-tse`، هر ۲ دقیقه ساعات بازار) که `Tsetmc/Index.php?type=1` را می‌خواند.
  `app/page.tsx` حالا هر سه کارت (شاخص کل، هم‌وزن، ارزش معاملات) را از همین جدول زنده می‌خواند —
  دیگر از `benchmark_candles`/جمع واچ‌لیست نه. برچسب «ارزش معاملات بازار (امروز)» به شکل اصلی‌اش
  برگشت چون حالا واقعاً درست است (نه محدود به واچ‌لیست).
- `build-candles` گسترش یافت: از تیک‌های امروزِ `market_index_quotes`، کندل روزانهٔ
  `benchmark_candles(tedpix/tedpix_equal_weight)` را با `lib/transforms/candle.ts::buildOhlcFromSeries`
  (تابع جدید، خالص، تست‌شده — عیناً همان اصل `buildDailyCandle` موجود) می‌سازد؛ یعنی از این پس
  `benchmark_candles` هم برای بک‌تست‌های آینده خودکار زنده می‌ماند، بدون نیاز به rerun دستی اسکریپت.
- **تأیید نهایی هنوز باقی است:** بعد از deploy و حداقل یک چرخهٔ کرون موفق در ساعات بازار، باید
  دوباره `index` واقعی را با tsetmc.com مقایسه کرد (انتظار: اختلاف زیر ۰.۱٪) — این کار در این
  session قابل انجام نبود چون بازار در لحظهٔ نوشتن این خط بسته بود.

## پرتفوی مجازی خودکار (Auto Paper-Trading) — در جریان

فیچر بزرگ خارج از نقشهٔ فازهای ۰-۱۰، حاکم بر آن **قید #۱۴** بالا (جریان یک‌طرفه، ممنوعیت
خودآموزی خودکار). ترتیب: بخش ۰ (تشخیص کارمزد) → ۱ (موتور اجرا) → ۲ (موتور عملکرد) →
۳ و ۴ (برچسب علت + گزارش) → ۵ و ۶ (تفکیک از دستی + بازبینی فصلی).

- [x] **بخش ۰ — تشخیص کارمزد (۲۰۲۶-۰۸-۱۲):** بررسی شد و معلوم شد کارمزد از اول در بک‌تست
      لحاظ بوده (`ROUND_TRIP_FEE=0.015`، در `closePosition` روی **هر معامله**، نه ضریب کلی
      نهایی) — یعنی همهٔ اعداد تاریخی گزارش‌شده از قبل خالص از کارمزد بودند و سناریوی
      «با/بدون کارمزد» موضوعیت نداشت. ولی چک نرخ جاری نشان داد عدد ۱.۵٪ کمی خوش‌بینانه است:
      نرخ واقعی ۱۴۰۵ حدود **۱.۷۵٪** است (خرید ~۰.۳۷٪ + فروش ~۰.۸۸٪ + مالیات فروش ۰.۵٪).
      تصمیم تأییدشدهٔ کاربر: ثابت بک‌تست دست‌نخورده بماند (مقایسه‌پذیری گزارش‌های تاریخی)، و
      پرتفوی مجازی با نرخ واقعی‌تر قابل‌تنظیم کار کند — جزئیات در قید #۱۴.
- [x] **بخش ۱ — موتور اجرای مجازی (۲۰۲۶-۰۸-۱۲، کامیت `6ca354f`):** جدول‌های
      `virtual_portfolio`/`virtual_trades` (مایگریشن `20260812120000`) + Edge Function
      `execute-virtual-trades` + کرون `5-59/10 5-9 * * 6,0,1,2,3` (مایگریشن `20260812120500`،
      عمداً ۵ دقیقه بعد از `compute-signals`). منطق خالص و تست‌شده در `lib/virtualExecution.ts`
      (کارمزد دوطرفه، `maxAffordableShares`، `decideBuyExecution`، انقضای صف، `realizedPnl`)
      و `lib/exitRules.ts` (قاعدهٔ خروج مشترک) و `lib/tradingDays.ts`. صف/اندازهٔ پوزیشن/حد ضرر
      همگی از زیرساخت موجود فاز ۸ (`queueState`، `calculatePositionSize`،
      `suggestStopLossFromAtr`) می‌آیند، بدون پیاده‌سازی موازی. ۲۴ تست جدید (جمعاً ۲۸۰).
      - **نقد از روی خود `virtual_trades` بازمحاسبه می‌شود، نه از ستون ذخیره‌شده** — اگر یک
        اجرا وسط کار بشکند، اجرای بعدی خودبه‌خود عدد درست را می‌سازد. `virtual_portfolio.cash`
        فقط cache نمایشی است.
      - **واگرایی آگاهانه و مستند بین بک‌تست و پرتفوی مجازی:** بک‌تست تاریخی هیچ حد ضرری ندارد
        (از فاز ۳)، ولی پرتفوی مجازی حد ضرر ATR14×۱.۵ فاز ۸ را اعمال می‌کند — یعنی اعداد این دو
        از این بابت مستقیماً قابل مقایسه نیستند. این تفاوت واقعی است، نه باگ (رجوع به کامنت
        بالای `lib/exitRules.ts`).
      - **اعتبارسنجی زندهٔ ناقص — صادقانه:** مایگریشن‌ها روی production اجرا و تابع deploy شد،
        ولی اجرای دستی در ۱۱:۰۹ UTC (بعد از بستن بازار در ۹:۳۰) فقط گارد بازار را لمس کرد و
        با `market_closed` ساکت خارج شد. یعنی اتصال/خواندن تنظیمات/گارد بازار/لاگ سلامت تأیید
        شدند، ولی **مسیر اصلی (خواندن سیگنال، اندازهٔ پوزیشن، کارمزد، صف، نقدینگی) هنوز با
        دادهٔ واقعی اجرا نشده است.**
      - ⏳ **کار باقی‌مانده برای جلسهٔ شنبه (۲۰۲۶-۰۸-۱۵، اولین روز معاملاتی بعدی):** اولین اجرای
        واقعی `execute-virtual-trades` در ساعات بازار باید بررسی شود — چند سیگنال دید، چند
        معامله اجرا/رد/در انتظار صف شد، و `pipeline_health` چه ثبت کرد. **این را فراموش نکن.**
        نکتهٔ زمینه‌ای: جلسهٔ ۲۰۲۶-۰۸-۱۲ اصلاً سیگنالی تولید نکرد (`compute-signals` چند بار
        `ok` با `"0 signals"`)، پس اگر شنبه هم صفر معامله دیده شد لزوماً باگ نیست.
- [ ] بخش ۲ — موتور محاسبهٔ عملکرد
- [ ] بخش ۳ و ۴ — برچسب‌گذاری علت + گزارش‌دهی
- [ ] بخش ۵ و ۶ — تفکیک از دادهٔ دستی + بازبینی فصلی انسان-در-حلقه
